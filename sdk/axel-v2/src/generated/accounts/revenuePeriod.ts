/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import { findRevenuePeriodPda, RevenuePeriodSeeds } from "../pdas";
import {
  getRevenueKindDecoder,
  getRevenueKindEncoder,
  type RevenueKind,
  type RevenueKindArgs,
} from "../types";

export const REVENUE_PERIOD_DISCRIMINATOR = new Uint8Array([
  56, 54, 193, 254, 205, 149, 212, 70,
]);

export function getRevenuePeriodDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REVENUE_PERIOD_DISCRIMINATOR,
  );
}

export type RevenuePeriod = {
  discriminator: ReadonlyUint8Array;
  project: Address;
  index: number;
  /** Period dates as YYYYMMDD, declared by the operator. */
  periodStart: number;
  periodEnd: number;
  gross: bigint;
  fee: bigint;
  net: bigint;
  /** Shares outstanding when the deposit was distributed. */
  supply: bigint;
  accAfter: bigint;
  /** SHA-256 of the period's P&L report, published off-chain. */
  reportHash: ReadonlyUint8Array;
  /** Oracle key that co-signed the deposit, attesting the report. */
  attestor: Address;
  /** Project telemetry chain head at the time of the deposit. */
  telemetryHead: ReadonlyUint8Array;
  kind: RevenueKind;
  /** Actual deposit time, so backfilled periods stay visible. */
  depositedAt: bigint;
  bump: number;
};

export type RevenuePeriodArgs = {
  project: Address;
  index: number;
  /** Period dates as YYYYMMDD, declared by the operator. */
  periodStart: number;
  periodEnd: number;
  gross: number | bigint;
  fee: number | bigint;
  net: number | bigint;
  /** Shares outstanding when the deposit was distributed. */
  supply: number | bigint;
  accAfter: number | bigint;
  /** SHA-256 of the period's P&L report, published off-chain. */
  reportHash: ReadonlyUint8Array;
  /** Oracle key that co-signed the deposit, attesting the report. */
  attestor: Address;
  /** Project telemetry chain head at the time of the deposit. */
  telemetryHead: ReadonlyUint8Array;
  kind: RevenueKindArgs;
  /** Actual deposit time, so backfilled periods stay visible. */
  depositedAt: number | bigint;
  bump: number;
};

/** Gets the encoder for {@link RevenuePeriodArgs} account data. */
export function getRevenuePeriodEncoder(): FixedSizeEncoder<RevenuePeriodArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["project", getAddressEncoder()],
      ["index", getU32Encoder()],
      ["periodStart", getU32Encoder()],
      ["periodEnd", getU32Encoder()],
      ["gross", getU64Encoder()],
      ["fee", getU64Encoder()],
      ["net", getU64Encoder()],
      ["supply", getU64Encoder()],
      ["accAfter", getU128Encoder()],
      ["reportHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["attestor", getAddressEncoder()],
      ["telemetryHead", fixEncoderSize(getBytesEncoder(), 32)],
      ["kind", getRevenueKindEncoder()],
      ["depositedAt", getI64Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: REVENUE_PERIOD_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link RevenuePeriod} account data. */
export function getRevenuePeriodDecoder(): FixedSizeDecoder<RevenuePeriod> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["project", getAddressDecoder()],
    ["index", getU32Decoder()],
    ["periodStart", getU32Decoder()],
    ["periodEnd", getU32Decoder()],
    ["gross", getU64Decoder()],
    ["fee", getU64Decoder()],
    ["net", getU64Decoder()],
    ["supply", getU64Decoder()],
    ["accAfter", getU128Decoder()],
    ["reportHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["attestor", getAddressDecoder()],
    ["telemetryHead", fixDecoderSize(getBytesDecoder(), 32)],
    ["kind", getRevenueKindDecoder()],
    ["depositedAt", getI64Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link RevenuePeriod} account data. */
export function getRevenuePeriodCodec(): FixedSizeCodec<
  RevenuePeriodArgs,
  RevenuePeriod
> {
  return combineCodec(getRevenuePeriodEncoder(), getRevenuePeriodDecoder());
}

export function decodeRevenuePeriod<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<RevenuePeriod, TAddress>;
export function decodeRevenuePeriod<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<RevenuePeriod, TAddress>;
export function decodeRevenuePeriod<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<RevenuePeriod, TAddress> | MaybeAccount<RevenuePeriod, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getRevenuePeriodDecoder(),
  );
}

export async function fetchRevenuePeriod<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<RevenuePeriod, TAddress>> {
  const maybeAccount = await fetchMaybeRevenuePeriod(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeRevenuePeriod<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<RevenuePeriod, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeRevenuePeriod(maybeAccount);
}

export async function fetchAllRevenuePeriod(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<RevenuePeriod>[]> {
  const maybeAccounts = await fetchAllMaybeRevenuePeriod(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeRevenuePeriod(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<RevenuePeriod>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeRevenuePeriod(maybeAccount));
}

export function getRevenuePeriodSize(): number {
  return 206;
}

export async function fetchRevenuePeriodFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: RevenuePeriodSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<Account<RevenuePeriod>> {
  const maybeAccount = await fetchMaybeRevenuePeriodFromSeeds(
    rpc,
    seeds,
    config,
  );
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeRevenuePeriodFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: RevenuePeriodSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<MaybeAccount<RevenuePeriod>> {
  const { programAddress, ...fetchConfig } = config;
  const [address] = await findRevenuePeriodPda(seeds, { programAddress });
  return await fetchMaybeRevenuePeriod(rpc, address, fetchConfig);
}
