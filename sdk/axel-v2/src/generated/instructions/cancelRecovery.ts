/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { AXEL_V2_PROGRAM_ADDRESS } from "../programs";

export const CANCEL_RECOVERY_DISCRIMINATOR = new Uint8Array([
  176, 23, 203, 37, 121, 251, 227, 83,
]);

export function getCancelRecoveryDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANCEL_RECOVERY_DISCRIMINATOR,
  );
}

export type CancelRecoveryInstruction<
  TProgram extends string = typeof AXEL_V2_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountRequest extends string | AccountMeta<string> = string,
  TAccountProposer extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountRequest extends string
        ? WritableAccount<TAccountRequest>
        : TAccountRequest,
      TAccountProposer extends string
        ? WritableAccount<TAccountProposer>
        : TAccountProposer,
      ...TRemainingAccounts,
    ]
  >;

export type CancelRecoveryInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CancelRecoveryInstructionDataArgs = {};

export function getCancelRecoveryInstructionDataEncoder(): FixedSizeEncoder<CancelRecoveryInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CANCEL_RECOVERY_DISCRIMINATOR }),
  );
}

export function getCancelRecoveryInstructionDataDecoder(): FixedSizeDecoder<CancelRecoveryInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCancelRecoveryInstructionDataCodec(): FixedSizeCodec<
  CancelRecoveryInstructionDataArgs,
  CancelRecoveryInstructionData
> {
  return combineCodec(
    getCancelRecoveryInstructionDataEncoder(),
    getCancelRecoveryInstructionDataDecoder(),
  );
}

export type CancelRecoveryAsyncInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountRequest extends string = string,
  TAccountProposer extends string = string,
> = {
  /** The admin or the request's `from_owner`. */
  authority: TransactionSigner<TAccountAuthority>;
  config?: Address<TAccountConfig>;
  request: Address<TAccountRequest>;
  proposer: Address<TAccountProposer>;
};

export async function getCancelRecoveryInstructionAsync<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TAccountRequest extends string,
  TAccountProposer extends string,
  TProgramAddress extends Address = typeof AXEL_V2_PROGRAM_ADDRESS,
>(
  input: CancelRecoveryAsyncInput<
    TAccountAuthority,
    TAccountConfig,
    TAccountRequest,
    TAccountProposer
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CancelRecoveryInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountRequest,
    TAccountProposer
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AXEL_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    request: { value: input.request ?? null, isWritable: true },
    proposer: { value: input.proposer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("config", accounts.config),
      getAccountMeta("request", accounts.request),
      getAccountMeta("proposer", accounts.proposer),
    ],
    data: getCancelRecoveryInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelRecoveryInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountRequest,
    TAccountProposer
  >);
}

export type CancelRecoveryInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountRequest extends string = string,
  TAccountProposer extends string = string,
> = {
  /** The admin or the request's `from_owner`. */
  authority: TransactionSigner<TAccountAuthority>;
  config: Address<TAccountConfig>;
  request: Address<TAccountRequest>;
  proposer: Address<TAccountProposer>;
};

export function getCancelRecoveryInstruction<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TAccountRequest extends string,
  TAccountProposer extends string,
  TProgramAddress extends Address = typeof AXEL_V2_PROGRAM_ADDRESS,
>(
  input: CancelRecoveryInput<
    TAccountAuthority,
    TAccountConfig,
    TAccountRequest,
    TAccountProposer
  >,
  config?: { programAddress?: TProgramAddress },
): CancelRecoveryInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountConfig,
  TAccountRequest,
  TAccountProposer
> {
  // Program address.
  const programAddress = config?.programAddress ?? AXEL_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    request: { value: input.request ?? null, isWritable: true },
    proposer: { value: input.proposer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("config", accounts.config),
      getAccountMeta("request", accounts.request),
      getAccountMeta("proposer", accounts.proposer),
    ],
    data: getCancelRecoveryInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelRecoveryInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountRequest,
    TAccountProposer
  >);
}

export type ParsedCancelRecoveryInstruction<
  TProgram extends string = typeof AXEL_V2_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The admin or the request's `from_owner`. */
    authority: TAccountMetas[0];
    config: TAccountMetas[1];
    request: TAccountMetas[2];
    proposer: TAccountMetas[3];
  };
  data: CancelRecoveryInstructionData;
};

export function parseCancelRecoveryInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelRecoveryInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      config: getNextAccount(),
      request: getNextAccount(),
      proposer: getNextAccount(),
    },
    data: getCancelRecoveryInstructionDataDecoder().decode(instruction.data),
  };
}
