/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { AXEL_V2_PROGRAM_ADDRESS } from "../programs";

export const CANCEL_RAISE_DISCRIMINATOR = new Uint8Array([
  93, 123, 204, 79, 107, 196, 120, 217,
]);

export function getCancelRaiseDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANCEL_RAISE_DISCRIMINATOR,
  );
}

export type CancelRaiseInstruction<
  TProgram extends string = typeof AXEL_V2_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountProject extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountProject extends string
        ? WritableAccount<TAccountProject>
        : TAccountProject,
      ...TRemainingAccounts,
    ]
  >;

export type CancelRaiseInstructionData = { discriminator: ReadonlyUint8Array };

export type CancelRaiseInstructionDataArgs = {};

export function getCancelRaiseInstructionDataEncoder(): FixedSizeEncoder<CancelRaiseInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CANCEL_RAISE_DISCRIMINATOR }),
  );
}

export function getCancelRaiseInstructionDataDecoder(): FixedSizeDecoder<CancelRaiseInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCancelRaiseInstructionDataCodec(): FixedSizeCodec<
  CancelRaiseInstructionDataArgs,
  CancelRaiseInstructionData
> {
  return combineCodec(
    getCancelRaiseInstructionDataEncoder(),
    getCancelRaiseInstructionDataDecoder(),
  );
}

export type CancelRaiseAsyncInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountProject extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config?: Address<TAccountConfig>;
  project: Address<TAccountProject>;
};

export async function getCancelRaiseInstructionAsync<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountProject extends string,
  TProgramAddress extends Address = typeof AXEL_V2_PROGRAM_ADDRESS,
>(
  input: CancelRaiseAsyncInput<TAccountAdmin, TAccountConfig, TAccountProject>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CancelRaiseInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountProject
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AXEL_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    project: { value: input.project ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
      getAccountMeta("project", accounts.project),
    ],
    data: getCancelRaiseInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelRaiseInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountProject
  >);
}

export type CancelRaiseInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountProject extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config: Address<TAccountConfig>;
  project: Address<TAccountProject>;
};

export function getCancelRaiseInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountProject extends string,
  TProgramAddress extends Address = typeof AXEL_V2_PROGRAM_ADDRESS,
>(
  input: CancelRaiseInput<TAccountAdmin, TAccountConfig, TAccountProject>,
  config?: { programAddress?: TProgramAddress },
): CancelRaiseInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountConfig,
  TAccountProject
> {
  // Program address.
  const programAddress = config?.programAddress ?? AXEL_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    project: { value: input.project ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
      getAccountMeta("project", accounts.project),
    ],
    data: getCancelRaiseInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelRaiseInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountProject
  >);
}

export type ParsedCancelRaiseInstruction<
  TProgram extends string = typeof AXEL_V2_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    config: TAccountMetas[1];
    project: TAccountMetas[2];
  };
  data: CancelRaiseInstructionData;
};

export function parseCancelRaiseInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelRaiseInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      config: getNextAccount(),
      project: getNextAccount(),
    },
    data: getCancelRaiseInstructionDataDecoder().decode(instruction.data),
  };
}
