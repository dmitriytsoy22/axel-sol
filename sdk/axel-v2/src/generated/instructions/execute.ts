/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda, findProjectPda } from "../pdas";
import { AXEL_V2_PROGRAM_ADDRESS } from "../programs";

export const EXECUTE_DISCRIMINATOR = new Uint8Array([
  105, 37, 101, 197, 75, 251, 102, 26,
]);

export function getExecuteDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(EXECUTE_DISCRIMINATOR);
}

export type ExecuteInstruction<
  TProgram extends string = typeof AXEL_V2_PROGRAM_ADDRESS,
  TAccountSource extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountDestination extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountExtraAccountMetas extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountProject extends string | AccountMeta<string> = string,
  TAccountSourceInvestor extends string | AccountMeta<string> = string,
  TAccountDestinationInvestor extends string | AccountMeta<string> = string,
  TAccountSourcePosition extends string | AccountMeta<string> = string,
  TAccountDestinationPosition extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSource extends string
        ? ReadonlyAccount<TAccountSource>
        : TAccountSource,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountDestination extends string
        ? ReadonlyAccount<TAccountDestination>
        : TAccountDestination,
      TAccountAuthority extends string
        ? ReadonlyAccount<TAccountAuthority>
        : TAccountAuthority,
      TAccountExtraAccountMetas extends string
        ? ReadonlyAccount<TAccountExtraAccountMetas>
        : TAccountExtraAccountMetas,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountProject extends string
        ? ReadonlyAccount<TAccountProject>
        : TAccountProject,
      TAccountSourceInvestor extends string
        ? ReadonlyAccount<TAccountSourceInvestor>
        : TAccountSourceInvestor,
      TAccountDestinationInvestor extends string
        ? ReadonlyAccount<TAccountDestinationInvestor>
        : TAccountDestinationInvestor,
      TAccountSourcePosition extends string
        ? WritableAccount<TAccountSourcePosition>
        : TAccountSourcePosition,
      TAccountDestinationPosition extends string
        ? WritableAccount<TAccountDestinationPosition>
        : TAccountDestinationPosition,
      ...TRemainingAccounts,
    ]
  >;

export type ExecuteInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type ExecuteInstructionDataArgs = { amount: number | bigint };

export function getExecuteInstructionDataEncoder(): FixedSizeEncoder<ExecuteInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: EXECUTE_DISCRIMINATOR }),
  );
}

export function getExecuteInstructionDataDecoder(): FixedSizeDecoder<ExecuteInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getExecuteInstructionDataCodec(): FixedSizeCodec<
  ExecuteInstructionDataArgs,
  ExecuteInstructionData
> {
  return combineCodec(
    getExecuteInstructionDataEncoder(),
    getExecuteInstructionDataDecoder(),
  );
}

export type ExecuteAsyncInput<
  TAccountSource extends string = string,
  TAccountMint extends string = string,
  TAccountDestination extends string = string,
  TAccountAuthority extends string = string,
  TAccountExtraAccountMetas extends string = string,
  TAccountConfig extends string = string,
  TAccountProject extends string = string,
  TAccountSourceInvestor extends string = string,
  TAccountDestinationInvestor extends string = string,
  TAccountSourcePosition extends string = string,
  TAccountDestinationPosition extends string = string,
> = {
  source: Address<TAccountSource>;
  mint: Address<TAccountMint>;
  destination: Address<TAccountDestination>;
  /** owners instead, so a delegate cannot move shares of an owner who lost KYC. */
  authority: Address<TAccountAuthority>;
  extraAccountMetas: Address<TAccountExtraAccountMetas>;
  config?: Address<TAccountConfig>;
  project?: Address<TAccountProject>;
  /** record fails as `SourceNotAllowed`. */
  sourceInvestor: Address<TAccountSourceInvestor>;
  destinationInvestor: Address<TAccountDestinationInvestor>;
  /** `destination_position` and Anchor does not reject duplicate mutable accounts. */
  sourcePosition: Address<TAccountSourcePosition>;
  destinationPosition: Address<TAccountDestinationPosition>;
  amount: ExecuteInstructionDataArgs["amount"];
};

export async function getExecuteInstructionAsync<
  TAccountSource extends string,
  TAccountMint extends string,
  TAccountDestination extends string,
  TAccountAuthority extends string,
  TAccountExtraAccountMetas extends string,
  TAccountConfig extends string,
  TAccountProject extends string,
  TAccountSourceInvestor extends string,
  TAccountDestinationInvestor extends string,
  TAccountSourcePosition extends string,
  TAccountDestinationPosition extends string,
  TProgramAddress extends Address = typeof AXEL_V2_PROGRAM_ADDRESS,
>(
  input: ExecuteAsyncInput<
    TAccountSource,
    TAccountMint,
    TAccountDestination,
    TAccountAuthority,
    TAccountExtraAccountMetas,
    TAccountConfig,
    TAccountProject,
    TAccountSourceInvestor,
    TAccountDestinationInvestor,
    TAccountSourcePosition,
    TAccountDestinationPosition
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ExecuteInstruction<
    TProgramAddress,
    TAccountSource,
    TAccountMint,
    TAccountDestination,
    TAccountAuthority,
    TAccountExtraAccountMetas,
    TAccountConfig,
    TAccountProject,
    TAccountSourceInvestor,
    TAccountDestinationInvestor,
    TAccountSourcePosition,
    TAccountDestinationPosition
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AXEL_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    source: { value: input.source ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    destination: { value: input.destination ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
    extraAccountMetas: {
      value: input.extraAccountMetas ?? null,
      isWritable: false,
    },
    config: { value: input.config ?? null, isWritable: false },
    project: { value: input.project ?? null, isWritable: false },
    sourceInvestor: { value: input.sourceInvestor ?? null, isWritable: false },
    destinationInvestor: {
      value: input.destinationInvestor ?? null,
      isWritable: false,
    },
    sourcePosition: { value: input.sourcePosition ?? null, isWritable: true },
    destinationPosition: {
      value: input.destinationPosition ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.project.value) {
    accounts.project.value = await findProjectPda({
      shareMint: getAddressFromResolvedInstructionAccount(
        "mint",
        accounts.mint.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("source", accounts.source),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("destination", accounts.destination),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("extraAccountMetas", accounts.extraAccountMetas),
      getAccountMeta("config", accounts.config),
      getAccountMeta("project", accounts.project),
      getAccountMeta("sourceInvestor", accounts.sourceInvestor),
      getAccountMeta("destinationInvestor", accounts.destinationInvestor),
      getAccountMeta("sourcePosition", accounts.sourcePosition),
      getAccountMeta("destinationPosition", accounts.destinationPosition),
    ],
    data: getExecuteInstructionDataEncoder().encode(
      args as ExecuteInstructionDataArgs,
    ),
    programAddress,
  } as ExecuteInstruction<
    TProgramAddress,
    TAccountSource,
    TAccountMint,
    TAccountDestination,
    TAccountAuthority,
    TAccountExtraAccountMetas,
    TAccountConfig,
    TAccountProject,
    TAccountSourceInvestor,
    TAccountDestinationInvestor,
    TAccountSourcePosition,
    TAccountDestinationPosition
  >);
}

export type ExecuteInput<
  TAccountSource extends string = string,
  TAccountMint extends string = string,
  TAccountDestination extends string = string,
  TAccountAuthority extends string = string,
  TAccountExtraAccountMetas extends string = string,
  TAccountConfig extends string = string,
  TAccountProject extends string = string,
  TAccountSourceInvestor extends string = string,
  TAccountDestinationInvestor extends string = string,
  TAccountSourcePosition extends string = string,
  TAccountDestinationPosition extends string = string,
> = {
  source: Address<TAccountSource>;
  mint: Address<TAccountMint>;
  destination: Address<TAccountDestination>;
  /** owners instead, so a delegate cannot move shares of an owner who lost KYC. */
  authority: Address<TAccountAuthority>;
  extraAccountMetas: Address<TAccountExtraAccountMetas>;
  config: Address<TAccountConfig>;
  project: Address<TAccountProject>;
  /** record fails as `SourceNotAllowed`. */
  sourceInvestor: Address<TAccountSourceInvestor>;
  destinationInvestor: Address<TAccountDestinationInvestor>;
  /** `destination_position` and Anchor does not reject duplicate mutable accounts. */
  sourcePosition: Address<TAccountSourcePosition>;
  destinationPosition: Address<TAccountDestinationPosition>;
  amount: ExecuteInstructionDataArgs["amount"];
};

export function getExecuteInstruction<
  TAccountSource extends string,
  TAccountMint extends string,
  TAccountDestination extends string,
  TAccountAuthority extends string,
  TAccountExtraAccountMetas extends string,
  TAccountConfig extends string,
  TAccountProject extends string,
  TAccountSourceInvestor extends string,
  TAccountDestinationInvestor extends string,
  TAccountSourcePosition extends string,
  TAccountDestinationPosition extends string,
  TProgramAddress extends Address = typeof AXEL_V2_PROGRAM_ADDRESS,
>(
  input: ExecuteInput<
    TAccountSource,
    TAccountMint,
    TAccountDestination,
    TAccountAuthority,
    TAccountExtraAccountMetas,
    TAccountConfig,
    TAccountProject,
    TAccountSourceInvestor,
    TAccountDestinationInvestor,
    TAccountSourcePosition,
    TAccountDestinationPosition
  >,
  config?: { programAddress?: TProgramAddress },
): ExecuteInstruction<
  TProgramAddress,
  TAccountSource,
  TAccountMint,
  TAccountDestination,
  TAccountAuthority,
  TAccountExtraAccountMetas,
  TAccountConfig,
  TAccountProject,
  TAccountSourceInvestor,
  TAccountDestinationInvestor,
  TAccountSourcePosition,
  TAccountDestinationPosition
> {
  // Program address.
  const programAddress = config?.programAddress ?? AXEL_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    source: { value: input.source ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    destination: { value: input.destination ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
    extraAccountMetas: {
      value: input.extraAccountMetas ?? null,
      isWritable: false,
    },
    config: { value: input.config ?? null, isWritable: false },
    project: { value: input.project ?? null, isWritable: false },
    sourceInvestor: { value: input.sourceInvestor ?? null, isWritable: false },
    destinationInvestor: {
      value: input.destinationInvestor ?? null,
      isWritable: false,
    },
    sourcePosition: { value: input.sourcePosition ?? null, isWritable: true },
    destinationPosition: {
      value: input.destinationPosition ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("source", accounts.source),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("destination", accounts.destination),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("extraAccountMetas", accounts.extraAccountMetas),
      getAccountMeta("config", accounts.config),
      getAccountMeta("project", accounts.project),
      getAccountMeta("sourceInvestor", accounts.sourceInvestor),
      getAccountMeta("destinationInvestor", accounts.destinationInvestor),
      getAccountMeta("sourcePosition", accounts.sourcePosition),
      getAccountMeta("destinationPosition", accounts.destinationPosition),
    ],
    data: getExecuteInstructionDataEncoder().encode(
      args as ExecuteInstructionDataArgs,
    ),
    programAddress,
  } as ExecuteInstruction<
    TProgramAddress,
    TAccountSource,
    TAccountMint,
    TAccountDestination,
    TAccountAuthority,
    TAccountExtraAccountMetas,
    TAccountConfig,
    TAccountProject,
    TAccountSourceInvestor,
    TAccountDestinationInvestor,
    TAccountSourcePosition,
    TAccountDestinationPosition
  >);
}

export type ParsedExecuteInstruction<
  TProgram extends string = typeof AXEL_V2_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    source: TAccountMetas[0];
    mint: TAccountMetas[1];
    destination: TAccountMetas[2];
    /** owners instead, so a delegate cannot move shares of an owner who lost KYC. */
    authority: TAccountMetas[3];
    extraAccountMetas: TAccountMetas[4];
    config: TAccountMetas[5];
    project: TAccountMetas[6];
    /** record fails as `SourceNotAllowed`. */
    sourceInvestor: TAccountMetas[7];
    destinationInvestor: TAccountMetas[8];
    /** `destination_position` and Anchor does not reject duplicate mutable accounts. */
    sourcePosition: TAccountMetas[9];
    destinationPosition: TAccountMetas[10];
  };
  data: ExecuteInstructionData;
};

export function parseExecuteInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedExecuteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 11) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 11,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      source: getNextAccount(),
      mint: getNextAccount(),
      destination: getNextAccount(),
      authority: getNextAccount(),
      extraAccountMetas: getNextAccount(),
      config: getNextAccount(),
      project: getNextAccount(),
      sourceInvestor: getNextAccount(),
      destinationInvestor: getNextAccount(),
      sourcePosition: getNextAccount(),
      destinationPosition: getNextAccount(),
    },
    data: getExecuteInstructionDataDecoder().decode(instruction.data),
  };
}
