/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findConfigPda,
  findInvestorPda,
  findPositionPda,
  findRecoveryRequestPda,
} from "../pdas";
import { AXEL_V2_PROGRAM_ADDRESS } from "../programs";

export const PROPOSE_RECOVERY_DISCRIMINATOR = new Uint8Array([
  15, 85, 115, 138, 219, 199, 133, 144,
]);

export function getProposeRecoveryDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PROPOSE_RECOVERY_DISCRIMINATOR,
  );
}

export type ProposeRecoveryInstruction<
  TProgram extends string = typeof AXEL_V2_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountProject extends string | AccountMeta<string> = string,
  TAccountFromOwner extends string | AccountMeta<string> = string,
  TAccountFromInvestor extends string | AccountMeta<string> = string,
  TAccountFromPosition extends string | AccountMeta<string> = string,
  TAccountToOwner extends string | AccountMeta<string> = string,
  TAccountToInvestor extends string | AccountMeta<string> = string,
  TAccountRequest extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountProject extends string
        ? ReadonlyAccount<TAccountProject>
        : TAccountProject,
      TAccountFromOwner extends string
        ? ReadonlyAccount<TAccountFromOwner>
        : TAccountFromOwner,
      TAccountFromInvestor extends string
        ? ReadonlyAccount<TAccountFromInvestor>
        : TAccountFromInvestor,
      TAccountFromPosition extends string
        ? ReadonlyAccount<TAccountFromPosition>
        : TAccountFromPosition,
      TAccountToOwner extends string
        ? ReadonlyAccount<TAccountToOwner>
        : TAccountToOwner,
      TAccountToInvestor extends string
        ? ReadonlyAccount<TAccountToInvestor>
        : TAccountToInvestor,
      TAccountRequest extends string
        ? WritableAccount<TAccountRequest>
        : TAccountRequest,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ProposeRecoveryInstructionData = {
  discriminator: ReadonlyUint8Array;
  shares: bigint;
  reasonHash: ReadonlyUint8Array;
};

export type ProposeRecoveryInstructionDataArgs = {
  shares: number | bigint;
  reasonHash: ReadonlyUint8Array;
};

export function getProposeRecoveryInstructionDataEncoder(): FixedSizeEncoder<ProposeRecoveryInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["shares", getU64Encoder()],
      ["reasonHash", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: PROPOSE_RECOVERY_DISCRIMINATOR }),
  );
}

export function getProposeRecoveryInstructionDataDecoder(): FixedSizeDecoder<ProposeRecoveryInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["shares", getU64Decoder()],
    ["reasonHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getProposeRecoveryInstructionDataCodec(): FixedSizeCodec<
  ProposeRecoveryInstructionDataArgs,
  ProposeRecoveryInstructionData
> {
  return combineCodec(
    getProposeRecoveryInstructionDataEncoder(),
    getProposeRecoveryInstructionDataDecoder(),
  );
}

export type ProposeRecoveryAsyncInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountProject extends string = string,
  TAccountFromOwner extends string = string,
  TAccountFromInvestor extends string = string,
  TAccountFromPosition extends string = string,
  TAccountToOwner extends string = string,
  TAccountToInvestor extends string = string,
  TAccountRequest extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Pays the request's rent, which returns to it when the request is executed or cancelled. */
  admin: TransactionSigner<TAccountAdmin>;
  config?: Address<TAccountConfig>;
  project: Address<TAccountProject>;
  fromOwner: Address<TAccountFromOwner>;
  fromInvestor?: Address<TAccountFromInvestor>;
  fromPosition?: Address<TAccountFromPosition>;
  toOwner: Address<TAccountToOwner>;
  toInvestor?: Address<TAccountToInvestor>;
  request?: Address<TAccountRequest>;
  systemProgram?: Address<TAccountSystemProgram>;
  shares: ProposeRecoveryInstructionDataArgs["shares"];
  reasonHash: ProposeRecoveryInstructionDataArgs["reasonHash"];
};

export async function getProposeRecoveryInstructionAsync<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountProject extends string,
  TAccountFromOwner extends string,
  TAccountFromInvestor extends string,
  TAccountFromPosition extends string,
  TAccountToOwner extends string,
  TAccountToInvestor extends string,
  TAccountRequest extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AXEL_V2_PROGRAM_ADDRESS,
>(
  input: ProposeRecoveryAsyncInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountProject,
    TAccountFromOwner,
    TAccountFromInvestor,
    TAccountFromPosition,
    TAccountToOwner,
    TAccountToInvestor,
    TAccountRequest,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ProposeRecoveryInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountProject,
    TAccountFromOwner,
    TAccountFromInvestor,
    TAccountFromPosition,
    TAccountToOwner,
    TAccountToInvestor,
    TAccountRequest,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AXEL_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    project: { value: input.project ?? null, isWritable: false },
    fromOwner: { value: input.fromOwner ?? null, isWritable: false },
    fromInvestor: { value: input.fromInvestor ?? null, isWritable: false },
    fromPosition: { value: input.fromPosition ?? null, isWritable: false },
    toOwner: { value: input.toOwner ?? null, isWritable: false },
    toInvestor: { value: input.toInvestor ?? null, isWritable: false },
    request: { value: input.request ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.fromInvestor.value) {
    accounts.fromInvestor.value = await findInvestorPda({
      wallet: getAddressFromResolvedInstructionAccount(
        "fromOwner",
        accounts.fromOwner.value,
      ),
    });
  }
  if (!accounts.fromPosition.value) {
    accounts.fromPosition.value = await findPositionPda({
      project: getAddressFromResolvedInstructionAccount(
        "project",
        accounts.project.value,
      ),
      owner: getAddressFromResolvedInstructionAccount(
        "fromOwner",
        accounts.fromOwner.value,
      ),
    });
  }
  if (!accounts.toInvestor.value) {
    accounts.toInvestor.value = await findInvestorPda({
      wallet: getAddressFromResolvedInstructionAccount(
        "toOwner",
        accounts.toOwner.value,
      ),
    });
  }
  if (!accounts.request.value) {
    accounts.request.value = await findRecoveryRequestPda({
      project: getAddressFromResolvedInstructionAccount(
        "project",
        accounts.project.value,
      ),
      fromOwner: getAddressFromResolvedInstructionAccount(
        "fromOwner",
        accounts.fromOwner.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
      getAccountMeta("project", accounts.project),
      getAccountMeta("fromOwner", accounts.fromOwner),
      getAccountMeta("fromInvestor", accounts.fromInvestor),
      getAccountMeta("fromPosition", accounts.fromPosition),
      getAccountMeta("toOwner", accounts.toOwner),
      getAccountMeta("toInvestor", accounts.toInvestor),
      getAccountMeta("request", accounts.request),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getProposeRecoveryInstructionDataEncoder().encode(
      args as ProposeRecoveryInstructionDataArgs,
    ),
    programAddress,
  } as ProposeRecoveryInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountProject,
    TAccountFromOwner,
    TAccountFromInvestor,
    TAccountFromPosition,
    TAccountToOwner,
    TAccountToInvestor,
    TAccountRequest,
    TAccountSystemProgram
  >);
}

export type ProposeRecoveryInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountProject extends string = string,
  TAccountFromOwner extends string = string,
  TAccountFromInvestor extends string = string,
  TAccountFromPosition extends string = string,
  TAccountToOwner extends string = string,
  TAccountToInvestor extends string = string,
  TAccountRequest extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Pays the request's rent, which returns to it when the request is executed or cancelled. */
  admin: TransactionSigner<TAccountAdmin>;
  config: Address<TAccountConfig>;
  project: Address<TAccountProject>;
  fromOwner: Address<TAccountFromOwner>;
  fromInvestor: Address<TAccountFromInvestor>;
  fromPosition: Address<TAccountFromPosition>;
  toOwner: Address<TAccountToOwner>;
  toInvestor: Address<TAccountToInvestor>;
  request: Address<TAccountRequest>;
  systemProgram?: Address<TAccountSystemProgram>;
  shares: ProposeRecoveryInstructionDataArgs["shares"];
  reasonHash: ProposeRecoveryInstructionDataArgs["reasonHash"];
};

export function getProposeRecoveryInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountProject extends string,
  TAccountFromOwner extends string,
  TAccountFromInvestor extends string,
  TAccountFromPosition extends string,
  TAccountToOwner extends string,
  TAccountToInvestor extends string,
  TAccountRequest extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AXEL_V2_PROGRAM_ADDRESS,
>(
  input: ProposeRecoveryInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountProject,
    TAccountFromOwner,
    TAccountFromInvestor,
    TAccountFromPosition,
    TAccountToOwner,
    TAccountToInvestor,
    TAccountRequest,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ProposeRecoveryInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountConfig,
  TAccountProject,
  TAccountFromOwner,
  TAccountFromInvestor,
  TAccountFromPosition,
  TAccountToOwner,
  TAccountToInvestor,
  TAccountRequest,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? AXEL_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    project: { value: input.project ?? null, isWritable: false },
    fromOwner: { value: input.fromOwner ?? null, isWritable: false },
    fromInvestor: { value: input.fromInvestor ?? null, isWritable: false },
    fromPosition: { value: input.fromPosition ?? null, isWritable: false },
    toOwner: { value: input.toOwner ?? null, isWritable: false },
    toInvestor: { value: input.toInvestor ?? null, isWritable: false },
    request: { value: input.request ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
      getAccountMeta("project", accounts.project),
      getAccountMeta("fromOwner", accounts.fromOwner),
      getAccountMeta("fromInvestor", accounts.fromInvestor),
      getAccountMeta("fromPosition", accounts.fromPosition),
      getAccountMeta("toOwner", accounts.toOwner),
      getAccountMeta("toInvestor", accounts.toInvestor),
      getAccountMeta("request", accounts.request),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getProposeRecoveryInstructionDataEncoder().encode(
      args as ProposeRecoveryInstructionDataArgs,
    ),
    programAddress,
  } as ProposeRecoveryInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountProject,
    TAccountFromOwner,
    TAccountFromInvestor,
    TAccountFromPosition,
    TAccountToOwner,
    TAccountToInvestor,
    TAccountRequest,
    TAccountSystemProgram
  >);
}

export type ParsedProposeRecoveryInstruction<
  TProgram extends string = typeof AXEL_V2_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Pays the request's rent, which returns to it when the request is executed or cancelled. */
    admin: TAccountMetas[0];
    config: TAccountMetas[1];
    project: TAccountMetas[2];
    fromOwner: TAccountMetas[3];
    fromInvestor: TAccountMetas[4];
    fromPosition: TAccountMetas[5];
    toOwner: TAccountMetas[6];
    toInvestor: TAccountMetas[7];
    request: TAccountMetas[8];
    systemProgram: TAccountMetas[9];
  };
  data: ProposeRecoveryInstructionData;
};

export function parseProposeRecoveryInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedProposeRecoveryInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 10) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 10,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      config: getNextAccount(),
      project: getNextAccount(),
      fromOwner: getNextAccount(),
      fromInvestor: getNextAccount(),
      fromPosition: getNextAccount(),
      toOwner: getNextAccount(),
      toInvestor: getNextAccount(),
      request: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getProposeRecoveryInstructionDataDecoder().decode(instruction.data),
  };
}
