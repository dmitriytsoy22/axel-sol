/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { AXEL_V2_PROGRAM_ADDRESS } from "../programs";
import {
  getTelemetryEntryDecoder,
  getTelemetryEntryEncoder,
  type TelemetryEntry,
  type TelemetryEntryArgs,
} from "../types";

export const RECORD_TELEMETRY_DISCRIMINATOR = new Uint8Array([
  42, 253, 255, 103, 185, 29, 208, 94,
]);

export function getRecordTelemetryDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RECORD_TELEMETRY_DISCRIMINATOR,
  );
}

export type RecordTelemetryInstruction<
  TProgram extends string = typeof AXEL_V2_PROGRAM_ADDRESS,
  TAccountOracle extends string | AccountMeta<string> = string,
  TAccountProject extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOracle extends string
        ? ReadonlySignerAccount<TAccountOracle> &
            AccountSignerMeta<TAccountOracle>
        : TAccountOracle,
      TAccountProject extends string
        ? WritableAccount<TAccountProject>
        : TAccountProject,
      ...TRemainingAccounts,
    ]
  >;

export type RecordTelemetryInstructionData = {
  discriminator: ReadonlyUint8Array;
  entries: Array<TelemetryEntry>;
};

export type RecordTelemetryInstructionDataArgs = {
  entries: Array<TelemetryEntryArgs>;
};

export function getRecordTelemetryInstructionDataEncoder(): Encoder<RecordTelemetryInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["entries", getArrayEncoder(getTelemetryEntryEncoder())],
    ]),
    (value) => ({ ...value, discriminator: RECORD_TELEMETRY_DISCRIMINATOR }),
  );
}

export function getRecordTelemetryInstructionDataDecoder(): Decoder<RecordTelemetryInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["entries", getArrayDecoder(getTelemetryEntryDecoder())],
  ]);
}

export function getRecordTelemetryInstructionDataCodec(): Codec<
  RecordTelemetryInstructionDataArgs,
  RecordTelemetryInstructionData
> {
  return combineCodec(
    getRecordTelemetryInstructionDataEncoder(),
    getRecordTelemetryInstructionDataDecoder(),
  );
}

export type RecordTelemetryInput<
  TAccountOracle extends string = string,
  TAccountProject extends string = string,
> = {
  oracle: TransactionSigner<TAccountOracle>;
  project: Address<TAccountProject>;
  entries: RecordTelemetryInstructionDataArgs["entries"];
};

export function getRecordTelemetryInstruction<
  TAccountOracle extends string,
  TAccountProject extends string,
  TProgramAddress extends Address = typeof AXEL_V2_PROGRAM_ADDRESS,
>(
  input: RecordTelemetryInput<TAccountOracle, TAccountProject>,
  config?: { programAddress?: TProgramAddress },
): RecordTelemetryInstruction<
  TProgramAddress,
  TAccountOracle,
  TAccountProject
> {
  // Program address.
  const programAddress = config?.programAddress ?? AXEL_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    oracle: { value: input.oracle ?? null, isWritable: false },
    project: { value: input.project ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("oracle", accounts.oracle),
      getAccountMeta("project", accounts.project),
    ],
    data: getRecordTelemetryInstructionDataEncoder().encode(
      args as RecordTelemetryInstructionDataArgs,
    ),
    programAddress,
  } as RecordTelemetryInstruction<
    TProgramAddress,
    TAccountOracle,
    TAccountProject
  >);
}

export type ParsedRecordTelemetryInstruction<
  TProgram extends string = typeof AXEL_V2_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    oracle: TAccountMetas[0];
    project: TAccountMetas[1];
  };
  data: RecordTelemetryInstructionData;
};

export function parseRecordTelemetryInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRecordTelemetryInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { oracle: getNextAccount(), project: getNextAccount() },
    data: getRecordTelemetryInstructionDataDecoder().decode(instruction.data),
  };
}
