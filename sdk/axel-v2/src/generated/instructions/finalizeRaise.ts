/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { AXEL_V2_PROGRAM_ADDRESS } from "../programs";

export const FINALIZE_RAISE_DISCRIMINATOR = new Uint8Array([
  207, 230, 200, 181, 254, 7, 85, 209,
]);

export function getFinalizeRaiseDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FINALIZE_RAISE_DISCRIMINATOR,
  );
}

export type FinalizeRaiseInstruction<
  TProgram extends string = typeof AXEL_V2_PROGRAM_ADDRESS,
  TAccountProject extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProject extends string
        ? WritableAccount<TAccountProject>
        : TAccountProject,
      ...TRemainingAccounts,
    ]
  >;

export type FinalizeRaiseInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type FinalizeRaiseInstructionDataArgs = {};

export function getFinalizeRaiseInstructionDataEncoder(): FixedSizeEncoder<FinalizeRaiseInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: FINALIZE_RAISE_DISCRIMINATOR }),
  );
}

export function getFinalizeRaiseInstructionDataDecoder(): FixedSizeDecoder<FinalizeRaiseInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getFinalizeRaiseInstructionDataCodec(): FixedSizeCodec<
  FinalizeRaiseInstructionDataArgs,
  FinalizeRaiseInstructionData
> {
  return combineCodec(
    getFinalizeRaiseInstructionDataEncoder(),
    getFinalizeRaiseInstructionDataDecoder(),
  );
}

export type FinalizeRaiseInput<TAccountProject extends string = string> = {
  project: Address<TAccountProject>;
};

export function getFinalizeRaiseInstruction<
  TAccountProject extends string,
  TProgramAddress extends Address = typeof AXEL_V2_PROGRAM_ADDRESS,
>(
  input: FinalizeRaiseInput<TAccountProject>,
  config?: { programAddress?: TProgramAddress },
): FinalizeRaiseInstruction<TProgramAddress, TAccountProject> {
  // Program address.
  const programAddress = config?.programAddress ?? AXEL_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    project: { value: input.project ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta("project", accounts.project)],
    data: getFinalizeRaiseInstructionDataEncoder().encode({}),
    programAddress,
  } as FinalizeRaiseInstruction<TProgramAddress, TAccountProject>);
}

export type ParsedFinalizeRaiseInstruction<
  TProgram extends string = typeof AXEL_V2_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    project: TAccountMetas[0];
  };
  data: FinalizeRaiseInstructionData;
};

export function parseFinalizeRaiseInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedFinalizeRaiseInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 1,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { project: getNextAccount() },
    data: getFinalizeRaiseInstructionDataDecoder().decode(instruction.data),
  };
}
