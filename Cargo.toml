[workspace]
members = [
    "programs/*"
]
resolver = "2"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1
[profile.release.build-override]
opt-level = 3
incremental = false
codegen-units = 1

[workspace.metadata.solana]
tools-version = "v1.52"

# Smaller ELF, cheaper deploy; most compute is spent in token program CPIs anyway.
[profile.release.package.axel-v2]
opt-level = "s"
